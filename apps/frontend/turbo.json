{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "env": ["VITE_MAP_KEY"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dotEnv": [".env.development.local"],
      "env": ["VITE_MAP_KEY"]
    }
  }
}
